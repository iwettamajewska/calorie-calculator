import React from "react";
import Option from "./Option";

const SelectYesorNo = () => {
  return (
    <select className="select">
      <Option />
    </select>
  );
};

export default SelectYesorNo;
